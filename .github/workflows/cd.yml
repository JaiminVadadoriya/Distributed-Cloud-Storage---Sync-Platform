name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types: [completed]
    branches: [main]

env:
  REGISTRY: ghcr.io
  API_IMAGE: ghcr.io/${{ github.repository }}/api
  CLIENT_IMAGE: ghcr.io/${{ github.repository }}/client

jobs:
  # ─────────────────────────────────────────────
  # Deploy to Staging
  # ─────────────────────────────────────────────
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    environment:
      name: staging
      url: ${{ vars.STAGING_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy to staging server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: ${{ secrets.STAGING_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          script: |
            cd /opt/cloud-storage
            docker compose pull
            docker compose up -d --remove-orphans
            docker system prune -f

  # ─────────────────────────────────────────────
  # Smoke Tests on Staging
  # ─────────────────────────────────────────────
  smoke-test:
    name: Smoke Tests
    runs-on: ubuntu-latest
    needs: deploy-staging

    steps:
      - name: Wait for Staging API readiness
        run: |
          echo "Waiting for staging API to be ready..."
          TIMEOUT=60
          INTERVAL=5
          ELAPSED=0
          until curl -s -f "${{ vars.STAGING_URL }}/health/ready" || [ $ELAPSED -eq $TIMEOUT ]; do
            echo "Staging API not ready yet... waiting $INTERVAL seconds."
            sleep $INTERVAL
            let ELAPSED=ELAPSED+INTERVAL
          done
          if ! curl -s -f "${{ vars.STAGING_URL }}/health/ready"; then
            echo "Timeout reached! Staging API is not ready."
            exit 1
          fi
          echo "Staging API is ready!"

      - name: Health check - API liveness
        run: |
          curl --fail --retry 5 --retry-delay 10 \
            ${{ vars.STAGING_URL }}/health/live

      - name: Health check - API readiness
        run: |
          curl --fail --retry 5 --retry-delay 10 \
            ${{ vars.STAGING_URL }}/health/ready

      - name: Health check - Client
        run: |
          curl --fail --retry 3 --retry-delay 5 \
            ${{ vars.STAGING_CLIENT_URL }}

  # ─────────────────────────────────────────────
  # Deploy to Production (manual approval)
  # ─────────────────────────────────────────────
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: smoke-test
    environment:
      name: production
      url: ${{ vars.PRODUCTION_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy to production server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.PRODUCTION_HOST }}
          username: ${{ secrets.PRODUCTION_USER }}
          key: ${{ secrets.PRODUCTION_SSH_KEY }}
          script: |
            cd /opt/cloud-storage
            docker compose pull
            docker compose up -d --remove-orphans
            docker system prune -f

      - name: Verify production health
        run: |
          echo "Waiting for production API to be ready..."
          TIMEOUT=60
          INTERVAL=5
          ELAPSED=0
          until curl -s -f "${{ vars.PRODUCTION_URL }}/health/ready" || [ $ELAPSED -eq $TIMEOUT ]; do
            echo "Production API not ready yet... waiting $INTERVAL seconds."
            sleep $INTERVAL
            let ELAPSED=ELAPSED+INTERVAL
          done
          if ! curl -s -f "${{ vars.PRODUCTION_URL }}/health/ready"; then
            echo "Timeout reached! Production API is not ready."
            exit 1
          fi
          echo "Production API is ready!"
