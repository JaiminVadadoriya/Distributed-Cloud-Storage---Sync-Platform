apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Release.Name }}-pdb
  labels:
    app: {{ .Release.Name }}-api
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: {{ .Release.Name }}-api
